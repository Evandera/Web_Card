# Rendering files to Flask
'''
We can render html files to flask by:
-- from flask import render_template
-- moving our html files to a directory called templates
-- return render_template('index.html')

We can insert local file or static file by:
-- moving our local file to a directory called static
-- set the file path to -> static/(file_name)
-- example: <link rel="stylesheet" href="static/styles.css">
'''
